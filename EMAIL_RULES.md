# Правила писем для рассылок — Timeweb

Единый свод правил, по которому мы собираем письма. Цель — один стиль во всех рассылках, лёгкое чтение и хорошие клики. Базовый каркас — `templates/base-email.html`.

> Живой документ. Финальные решения зафиксированы по итогам письма 1 (велком-цепочка). Новые решения — дописываем сюда.

---

## 1. Основы

- **Язык:** русский.
- **Обращение:** на «вы» (нейтрально-дружелюбно).
- **Бренд:** Timeweb / Таймвэб.Технологии. Футер и бренд-константы — из шаблона (см. раздел в конце).
- **Голос:** простой, человеческий. Короткие предложения, активный залог, конкретика. Без канцелярита и штампов («спешим сообщить»). Без агрессивных продаж — объясняем пользу.

## 2. Дизайн-подход: адаптируем, а не копируем ТЗ

ТЗ задаёт **смысл и содержание**, а не вёрстку. Собираем как профессиональный дизайнер:

- **Не повторяем структуру ТЗ 1:1.** Перегруппировываем по логике, убираем дублирование и логические ошибки.
- **Группируем по сценариям/аудиториям** (например, «новичок» и «уже есть сайты» — разные пути, не смешиваем).
- **Главную конверсию выделяем** отдельным акцентным блоком, не прячем в общем списке.
- **Балансируем копирайт:** в карточках одного ряда — примерно одинаковый объём текста (одинаковое число строк), иначе «грязно».
- **Ровные сетки:** элементы в ряд — это карточки (фон/обводка), контент сбалансирован; не колонки с рваным выравниванием и разным объёмом текста.
- Сокращаем и переписываем формулировки ради ясности и ритма (смысл сохраняем).

## 3. Тема и прехедер

- **Тема:** до 40–50 символов. Конкретная польза/интрига, без обмана. Без КАПСА и спам-слов. Максимум 1 уместный эмодзи.
- **Прехедер:** до 90 символов, дополняет тему (не дублирует). В коде — скрытый блок `.mail_preheader`.

## 4. Структура письма

1. **Hero (тёмная шапка)** — лого + H1 (главная мысль) + подзаголовок + одна кнопка.
2. **Лид/контент-блоки** — суть по сценариям, короткие абзацы, карточки, списки.
3. **Акцентный блок конверсии** — главный оффер (например, годовой тариф) отдельно.
4. **Доверие / соц. доказательство** — при необходимости.
5. **Блок поддержки** (Телеграм, 24/7) + **футер** (контакты, причина письма, отписка).

Несколько CTA в письме — это нормально (стиль бренда). Не загоняем себя в «один CTA на письмо».

## 5. Кнопки (CTA) — фиксировано

- Текст: глагол + польза. Не «Нажмите здесь».
- **Скругление 16px.** Шрифт `Helvetica, Roboto, Arial`, **bold 17px**, паддинг ~15px (десктоп) / 14px (мобайл).
- **Ширина:** во всю ширину контентного блока — **кроме hero**.
- **Hero — всегда одна кнопка primary, hug-content** (по ширине текста).
- **Две кнопки → стопкой:** primary сверху, secondary снизу, разделитель 12px (не в ряд).
- **Primary:** заливка `#2784CE`, белый текст.
- **Secondary:** **solid** — заливка `#EBF6FF`, текст `#2784CE`, без рамки (предпочтительно). Альтернатива — контур `1px #2784CE`. На тёмном фоне: контур светлее/текст белый.

## 6. Визуальная система

### Цвета
| Назначение | HEX |
|---|---|
| Фон страницы | `#E8EBF0` |
| Тёмная шапка / акцент | `#13264A` (градиенты `#243f62→#0f203a`, `#13264A→#144261`) |
| Основная кнопка/акцент | `#2784CE` |
| Основной текст | `#1b364a` |
| Вторичный текст (описания) | `#676e85` |
| Приглушённый на тёмном | `#8e9bb0` / `#a2a9b4` |
| Обводка карточек | `#E3E8F0` |
| Светло-голубой чип/secondary | `#EBF6FF` |
| Ссылка | `#2784CE` (раньше `#3FA0EE`) |
| Текст футера | `#676e85`, мелкий `#99a0b8` |

### Типографика и геометрия
- **Шрифт:** `Arial, Helvetica, sans-serif`.
- **Размеры:** H1 32px (hero), H3-заголовки блоков 22px, body 17px (lh 28px), описания карточек 14–15px (lh 21–22px), футер 14–15px.
- **Ширина письма:** 600px.
- **Скругления (дружелюбный стиль):** блоки **26px**, карточки-фичи **22–24px**, кнопки **16px**, иконки-чипы — круглые (`border-radius:50%`). **Слоты под картинки — без скругления (прямые углы).**
- **Паддинг блоков:** ~36px (тёмный акцент ~32px).
- **Разделитель между блоками:** пустой `div` 12px (сверху письма 12+12).

### Карточки-фичи
- **Белый фон + обводка `1px #E3E8F0`** (предпочтительно вместо серой заливки `#F4F7FB`).
- **Карточки в одном ряду — равной высоты:** балансируем число строк текста **+** у вложенной таблицы `height: 100%`.

### Иконки — цветовое кодирование (Phosphor fill)
- Пак **Phosphor Icons**, вариант **fill** (MIT). Не рисуем вручную. Фетч: `https://cdn.jsdelivr.net/npm/@phosphor-icons/core@2/assets/fill/<name>-fill.svg`.
- Каждая фича — **свой цвет**; значок в **круглом** чипе, фон чипа — светлый тон того же цвета.
- Палитра кодирования:
  | Цвет | Значок | Чип |
  |---|---|---|
  | синий | `#2784CE` | `#EAF2FC` |
  | фиолетовый | `#7A5AF8` | `#EFECFD` |
  | янтарный | `#F2920C` | `#FDF1DF` |
  | зелёный | `#12B76A` | `#E6F6EE` |
- На **тёмном** фоне: чип — белый с прозрачностью `rgba(255,255,255,0.10)` (фолбэк-hex `#2A3B5C`), значок **белый**.
- Размер чипа: ~56–72px, значок ~30–36px (svg `viewBox="0 0 256 256"`).

### 3D-иллюстрации (опционально)
- Пак **Microsoft Fluent Emoji** (3D, MIT), фетчим сами: `https://cdn.jsdelivr.net/gh/microsoft/fluentui-emoji@main/assets/<Имя>/3D/<имя>_3d.png` (256×256, прозрачный фон).
- Стиль «эмодзи-3D» (цветной) не всегда совпадает с фирменным синим 3D — применять осознанно. **Чистый hero без иллюстрации — допустимый и часто более «дорогой» вариант.**

### Картинки и слоты
- Где иллюстрация усиливает блок — ставим **слот-плейсхолдер**: блок нужного размера, подпись `IMG W×H`, HTML-комментарий `<!-- IMG: ... заменить на <img ...> -->`. **Углы прямые.** Декоративные — прячем на мобиле (`mob_hidden`).
- У всех картинок — `alt`. Картинки не несут критичный смысл в одиночку (грузятся не всегда).
- **Превью:** для локального просмотра картинки встраиваем base64 (панель не видит папку `img/`). **Для отправки** — заливаем в `img/` → на CDN, заменяем на полные URL. Gmail/Outlook **вырезают** инлайн-SVG и data-URI — в боевом письме только хостинг-URL (PNG).

## 7. Готовые паттерны блоков (как в письме 1)

- **Hero (тёмный):** градиент `#13264A`, радиус 26px. Лого (32px) → отступ ~28px → H1 32px белый → подзаголовок 18px `#8e9bb0` → одна hug-кнопка primary. Без иллюстрации либо со слотом справа (`mob_hidden`).
- **Блок 3 фичи в ряд:** белый блок; внутри 3 карточки (белый + обводка), контент **по центру**, сверху круглый цветной чип-иконка, заголовок 16px bold, описание 14px `#676e85`. На мобиле — стопкой (`mob-col`).
- **Список-выбор (кликабельные карточки):** каждая позиция — отдельная карточка (белый + обводка, радиус 20px), **вся карточка — ссылка** (`<a style="display:block;text-decoration:none">`). Слева круглый цветной чип-иконка, по центру заголовок (`#1b364a` bold) + описание (`#676e85`), справа по центру стрелка `→` (`#2784CE`, 22px). Все ячейки `valign="middle"`. _Outlook (Win): кликабельным будет контент, не вся плашка целиком — известное ограничение._
- **Блок доверия 2×2:** белые карточки с обводкой, `height:100%`, круглый цветной чип-иконка (~56px), заголовок 17px + описание 14px `#676e85`; тексты сбалансированы по строкам.
- **Блок конверсии (светлый):** белый блок как остальные (радиус 26px). Заголовок 22px `#1b364a` + описание `#676e85`; справа цветной **круглый** чип-иконка (как у фич, напр. подарок — амбер `#F2920C`/`#FDF1DF`, `mob_hidden`); кнопка primary **hug-content**.
- **Промо-баннер (опционально):** полноширинный слот `IMG ~1200×400` в белом блоке. Можно убирать.
- **Блок поддержки + футер:** из шаблона как есть.

## 8. Адаптив и почтовики

- **Mobile-first**, проверяем на телефоне.
- Вёрстка на таблицах, MSO-условные комментарии для Outlook, классы `.mob-*`, `.mob_hidden`, `.only_mob`. Структуру таблиц меняем аккуратно.
- 3 колонки/2×2 на мобиле — в стопку (`.mob-col`).

## 9. Обязательные элементы (юридические/технические)

- Узнаваемые `From` (имя + адрес отправителя).
- **Ссылка на отписку** в футере (`{{UnsubscribeUrl}}`) — обязательно.
- Причина получения письма (особенно для холодных/B2B).
- Текстовая версия (plain text fallback) для боевой рассылки.

## 10. Чек-лист перед отправкой

- [ ] Тема ≤ 50 символов, без капса/спам-слов; прехедер заполнен и не дублирует тему
- [ ] CTA понятны; формат кнопок по правилам (hero — одна hug; остальные — во всю ширину)
- [ ] Карточки в рядах равной высоты, тексты сбалансированы
- [ ] Иконки из Phosphor, цветовое кодирование консистентно
- [ ] Картинки залиты на CDN, в `src` — полные URL (не локальные пути, не base64, не SVG)
- [ ] У всех картинок `alt`; ссылки рабочие (+ UTM при необходимости)
- [ ] Есть ссылка на отписку
- [ ] Проверено на мобильном и (по возможности) в Outlook
- [ ] Прочитал вслух — звучит как живой человек; нет опечаток

---

## Бренд-константы

**Контакты / футер (из шаблона):**
- **Телефон:** 8 (800) 700 19 53
- **Поддержка:** Telegram, подпись «На связи 24/7».
- **Футер-ссылки:** Партнёрская программа · Community в Telegram · База знаний.
- **Отписка:** `{{UnsubscribeUrl}}`.
- **Копирайт:** «ТАЙМВЭБ.ТЕХНОЛОГИИ» + ссылка на политику обработки перс. данных.

**Шрифты кнопок:** `Helvetica, Roboto, Arial, sans-serif`.

## Базовый шаблон `templates/base-email.html`

Готовые блоки (включаем/убираем под письмо): прехедер · шапка с фоном · шапка с кнопкой и иконкой · текстовый блок · текстовый блок с картинкой/чеклистом/кнопкой · финальный (тёмный) блок · блок поддержки (десктоп/мобайл) · футер. Всё на таблицах с MSO-комментариями и адаптивными классами.

## Структура файлов

- `EMAIL_RULES.md` — этот свод правил.
- `templates/base-email.html` — исходный каркас.
- `Велком-цепочка (1 месяц)/` — письма цепочки (`Письмо 1.html` …) + `img/` (ассеты).
