# Concept: design v2

> ⚠️ Это **экспериментальный концепт**, отдельный от боевых правил.
> Боевой свод правил — `EMAIL_RULES.md` (его трогать нельзя).
> Эталонный референс — `Возврат на годовой тариф/Верните годовой тариф (designv2).html`.

Этот файл фиксирует решения по дизайну, которые приняли итеративно. Если делаем ещё одно письмо в стиле designv2 — сверяемся с ним.

---

## 1. Палитра

### Фоны
| Назначение | Цвет |
|---|---|
| Страница (за пределами карточек) | `#E8EBF0` |
| Hero (большой тёмный блок сверху) | `#04080D` |
| Hero — радиальный градиент в правом нижнем углу | `radial-gradient(circle at 100% 100%, #0E2238 0%, #04080D 55%)` |
| Основные блоки (Плюсы / Подарки / Поможем разобраться) | `#FFFFFF` |
| Карточки и ссылки-строки внутри блоков | `#F4F6F9` (без обводки) |
| Кружок под иконкой в карточке | `#FFFFFF` |
| Кружок под иконкой в hero | `#1b364a` |
| Footer (на странице, не внутри карточки) | `#E8EBF0` (тот же что у страницы) |

### Текст
| Назначение | Цвет |
|---|---|
| Основной текст по всему письму | `#1b364a` |
| Заголовок hero (h1) | `#FFFFFF` |
| Descr в hero | `#8e9bb0` (единственное место с приглушённым; на тёмном иначе не читается) |
| Текст белой кнопки hero | `#04080D` (как фон hero) |
| Текст синих кнопок | `#FFFFFF` |
| Стрелка в логотипе Timeweb | `#5492C4` |

### Акценты
| Назначение | Цвет |
|---|---|
| Акцентный синий (кнопки, основные элементы) | `#2784CE` |
| Светло-голубой (подложка duotone-иконок) | `#BFD9F2` |

**Важно:** приглушённых текстов (`#676e85`, `#99a0b8` и т.п.) на светлых фонах **нет** — всё основным `#1b364a`.

---

## 2. Иконки

**Пак:** Solar Bold Duotone (iconify: `solar:<name>-bold-duotone`).

### В карточках (на светлом фоне)
- Кружок-подложка: `bgcolor="#FFFFFF"`, 56×56, `border-radius:50%`.
- SVG: 30×30, `viewBox="0 0 24 24"`.
- Цвета:
  - **BG-путь** (был `opacity=".5"` у Solar): `fill="#BFD9F2"` (светло-голубой).
  - **FG-путь** (наследует от svg): `fill="#2784CE"` (акцентный синий).

### В hero (на тёмном фоне)
- Кружок-подложка: `bgcolor="#1b364a"`, 120×120, `border-radius:50%`.
- SVG: 64×64, `viewBox="0 0 24 24"`.
- Цвета **инвертированы** относительно карточек:
  - **BG-путь**: `fill="#2784CE"` (акцентный — становится подложкой).
  - **FG-путь**: `fill="#BFD9F2"` (светлый — становится деталью).

---

## 3. Типографика

Шрифт: `Arial, Helvetica, sans-serif` для текста; кнопки — `Helvetica, Roboto, Arial, sans-serif`.

| Элемент | Размер / line-height / weight |
|---|---|
| Hero h1 | 30 / 34 / 700 |
| Hero descr (тег `<p>`, не `<h2>`) | 15 / 23 / 400 |
| Заголовок блока h3 | 22 / 28 / 700 |
| Вводный абзац блока | 17 / 28 / 400 |
| Заголовок карточки | 17 / 24 / 700 |
| Тело карточки | 14 / 21 / 400 |
| Заголовок ссылки-строки | 17 / 24 / 700 |
| Описание ссылки-строки | 15 / 22 / 400 |
| Кнопка | 17 / 18 / 700 |
| Footer (соц/политика/телефон) | как в EMAIL_RULES.md (не трогали) |

**Важно для descr в hero:** обязательно `<p>` + `font-weight: 400`, иначе клиенты типа Gmail/Outlook отрисуют `<h2>` жирным.

В медиа-запросе мобилки:
```css
.mob-h2 { font-size: 14px !important; line-height: 22px !important; font-weight: 400 !important; }
```

---

## 4. Скругления (border-radius)

Иерархия — крупный блок крупнее, мелкий мельче:

| Элемент | Радиус |
|---|---|
| Основные блоки (hero, white-блоки) | 16px |
| Карточки внутри блоков | 14px |
| Ссылки-строки в «Поможем разобраться» | 12px |
| Кнопки | 10px |
| Кружки иконок | 50% |

---

## 5. Hero (большой тёмный блок)

**Структура:** две колонки.

```
┌──────────────────────────────────────────┐
│ [logo]                                   │
│                                          │
│ Заголовок                                │
│ Описание (короткое, до правой колонки)   │
│                                          │
│ [белая кнопка]              [big icon]   │
└──────────────────────────────────────────┘
```

- Внешний `<td>`: `padding: 36px` (равномерный), `border-radius: 16px`, `bgcolor="#04080D"` + `background-image: radial-gradient(...)`.
- Внутри — таблица 2 колонки:
  - **Левая:** `width="62%"` (или `72%` для более узкой иконки), `valign="top"`, `padding-right: 16px`. Содержит логотип → h1 → descr → кнопку.
  - **Правая:** `width="38%"` (или `28%`), `valign="bottom"`, `align="right"`. Содержит кружок 120×120 с иконкой.
- Обе td с классом `mob-col` — на мобиле колонки сложатся вертикально.

**Логотип:** inline SVG `Timeweb` (текст белый, стрелка `#5492C4`), 102×26.

**Кнопка hero:** белый фон `#FFFFFF`, текст `#04080D`, `padding: 15px 30px`. (В отличие от синих кнопок в других блоках.)

**Иконка hero:** должна тематически отражать суть письма (для «Экономия 35%» — `sale-square`).

---

## 6. Основные блоки (Плюсы, Подарки, Поможем)

- Внешний `<td>`: `bgcolor="#FFFFFF"`, `padding: 36px`, `border-radius: 16px`.
- Заголовок h3 + (опционально) вводный `<p>`.
- Сетка карточек 2×2 (или 2×1) либо вертикальный список ссылок-строк.
- Кнопка (опционально) внизу — синяя.

### Карточки внутри блока
- `bgcolor="#F4F6F9"`, `border-radius: 14px`, **без обводки**.
- Padding td: `24px`.
- Структура: иконка-кружок → spacer 12px → заголовок 17/24 bold → spacer 6px → текст 14/21 regular.

### Ссылки-строки (как в «Поможем разобраться»)
- `bgcolor="#F4F6F9"`, `border-radius: 12px`, **без обводки**.
- Горизонтальная таблица: иконка-кружок 56×56 (с padding `18px 0 18px 18px`) | контент (заголовок 17/24 bold + описание 15/22 regular) | стрелка `→` (`#2784CE`, 22px bold) справа.

---

## 7. Кнопки

| Контекст | Фон | Текст | Padding |
|---|---|---|---|
| Hero | `#FFFFFF` | `#04080D` | `15px 30px` |
| Остальные блоки | `#2784CE` | `#FFFFFF` | `15px` (full-width) или `15px 30px` |

`border-radius: 10px`, `font-weight: 700`, `font-size: 17px`, `line-height: 18px`.

В медиа-запросе мобилки:
```css
.mob-button-border { border-radius: 10px !important; }
.mob-button-size { font-size: 16px !important; line-height: 16px !important; padding: 14px 20px !important; }
```

---

## 8. Отступы между блоками
Между всеми основными блоками — спейсер 12px:
```html
<div style="height: 12px; line-height: 12px; font-size: 10px;">&nbsp;</div>
```

Перед футером — 28px.

---

## 9. Preview vs Production

Inline SVG (логотип + иконки) поддерживают далеко не все клиенты: Gmail и Outlook вырезают `<svg>` целиком, Яндекс/Mail.ru ненадёжно. Поэтому для каждого designv2-письма держим **две версии**:

- `... (designv2).html` — preview-версия с inline SVG (то, что в iframe вьювере, что мы редактируем итерационно).
- `... (designv2 production).html` — боевая версия, в которой все `<svg>` заменены на `<img src="https://leonby27.github.io/TWMails/img/...png">`.

PNG-иконки лежат в `img/` в корне репо. Текущий пайплайн генерации (см. `/tmp/svg2png/gen.js`, должен переехать в `scripts/`):
1. Render SVG → PNG через `sharp` (Node), density 384, ресайз до 3× от display-размера.
2. Card-иконки: 30×30 displayed → 90×90 PNG.
3. Hero-иконка: 64×64 displayed → 192×192 PNG.
4. Логотип: 102×26 displayed → 306×78 PNG.
5. Скрипт `build-prod.js` берёт preview-HTML, регексами заменяет каждый `<svg>` на `<img>` с абсолютным URL → пишет production-файл.

**Что лежит в `index.html`:** в нав-сайдбаре триточки → дропдаун «Скачать». Для designv2 ссылка ведёт на production-файл, а превью в iframe — на preview-файл с SVG.

**Перед реальной рассылкой:** хосты иконок надо переехать на постоянный CDN рассылок (`img.emlacc.com` или аналог), не на GitHub Pages. Обновить `BASE` в `build-prod.js` и перегенерить production-файлы.

---

## 10. Чек-лист перед сдачей письма в стиле designv2
- [ ] Hero на `#04080D` + радиальный градиент.
- [ ] Логотип в hero — inline SVG (белый текст + синяя стрелка).
- [ ] Hero descr через `<p>` с `font-weight: 400` (не `<h2>`).
- [ ] В hero — 2 колонки, кружок-иконка справа внизу 120×120 на `#1b364a` с инвертированной палитрой.
- [ ] Кнопка в hero — белая, текст `#04080D`.
- [ ] Кнопки в остальных блоках — синие `#2784CE`.
- [ ] Все иконки — Solar Bold Duotone, цвета `BFD9F2` / `2784CE` (в карточках) и инверсия в hero.
- [ ] Карточки — `#F4F6F9` без обводки.
- [ ] Иконочные кружки в карточках — белые.
- [ ] Никаких приглушённых текстов на светлом — только `#1b364a`.
- [ ] Скругления: 16 / 14 / 12 / 10 / 50%.
